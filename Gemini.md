javascript:(function(){function t(e){return (e.innerText||'').trim()}let r=document.querySelector('main')||document.body;let ps=[...r.querySelectorAll('p,pre,li,blockquote,article,section')].map(n=>t(n)).filter(x=>x.length>0);if(!ps.length){alert('No text found. Scroll the chat first.');return}let md='# Gemini Chat Export\n\n'+ps.join('\n\n');let blob=new Blob([md],{type:'text/markdown'});let a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='gemini-chat.md';a.click();URL.revokeObjectURL(a.href)})();